import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ptc.app',
  appName: 'PTC 2',
  // Next.js static export directory - run `npm run export` before building
  webDir: 'out'
};

export default config;
